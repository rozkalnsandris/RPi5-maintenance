# ADR-0005: Production pins immutable maintenance releases

Status: Accepted.

Decision: production never tracks arbitrary `main`. Installation/deployment is bound to an exact reviewed release/commit and is a separate authorization from merge.
