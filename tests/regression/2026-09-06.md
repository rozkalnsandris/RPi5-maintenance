# Regression specification: 2026-09-06

Fixture inputs:

- APT succeeds.
- Main candidate pull completes or progresses with a large image.
- Main Compose reconcile returns non-zero and preserves exact stderr.
- The final main stack converges to healthy state.
- A dependent MQTT consumer experiences temporary broker unavailability.
- CV continuation is decided from shared-infrastructure state.
- No reboot-required signal exists.

Assertions:

1. exact first Docker failure is present in evidence;
2. mutation state is known, not inferred from exit code;
3. final health is independently verified;
4. failure classification distinguishes command failure from healthy system;
5. no unbounded restart storm occurs;
6. doctor action budget cannot loop indefinitely;
7. reboot is not performed when not required;
8. final report names phase, reason, mutation state, final health and evidence ID.
