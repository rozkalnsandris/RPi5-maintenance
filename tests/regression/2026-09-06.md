# Regression specification: 2026-09-06

The executable fixture is `tests/fixtures/docker-maintenance-2026-09-06.env` and is exercised by `tests/test-maintenance-updater-docker-evidence.sh`.

## Evidence-backed fixture

- APT Docker packages are upgraded and the Docker daemon restarts.
- Candidate image pull completes.
- One registry-backed main service (`mosquitto`) is still not running when post-pull target selection occurs.
- Target selection returns `2` with reason `missing-running-container` and service `mosquitto`.
- Compose reconcile has not started at that failure point.

## Assertions

1. the first known post-pull failure phase is `target-selection`;
2. the structured reason and affected service are retained;
3. command evidence preserves stdout/stderr and the exact command exit code;
4. timeout/readiness evidence is distinguishable from an ordinary command failure;
5. immutable image/container snapshots can report `none`, `changed` or `unknown` mutation evidence;
6. final Compose health is captured independently from update-command success;
7. the fixture does not perform production Docker, APT, systemd or network mutation;
8. no new remediation/restart policy is introduced by P1.

The historical V27 run cannot prove its exact internal return statement because that evidence was not retained. The fixture therefore tests the reproduced source path consistent with the preserved host evidence rather than rewriting history as certainty.
